# Archive

本目录保留项目历史产物和旧版文件。

## 说明

项目早期的索引、规划、素材映射文件保留在根目录的 `outputs/`、`samples/` 和 `topic_build_full/` 中。本 archive 目录为新产生的、需要归档但不属于当前活跃交付物的文件预留。

如需追溯专题建设历史，请参见 `topic_learning_packages/reports/topic_build_log.md`。
